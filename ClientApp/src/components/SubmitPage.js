import React, { Component } from 'react';

export default class SubmitPage extends Component {
    static displayName = SubmitPage.name;

    render() {
        return (
            <p>submit page</p>);
    }
}
